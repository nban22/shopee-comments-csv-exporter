import { NextFunction, Request, Response } from "express";
import { catchAsync } from "../utils/catchAsync";

export const analyzeText = catchAsync(async (req: Request, res: Response, next: NextFunction) => {
    
})